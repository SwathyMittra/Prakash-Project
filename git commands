git commands:
1) git pull origin master
2) git checkout -b branch_name
3) git checkout branch_name
4) git status (Staged changes will be shown)
5) git commit -m "commit_message"
6) git push

Finally, create a PR (Pull request)
